// Likes routes are mounted inside scripts.routes.js (as nested /:id/like etc.)
// This file exists for symmetry and possible standalone usage in future.
import { Router } from 'express';
const router = Router();
export default router;
